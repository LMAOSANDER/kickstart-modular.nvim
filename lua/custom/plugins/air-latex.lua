return {
  --  'dmadisetti/AirLatex.vim',
  --  build = ':UpdateRemotePlugins',
  --  lazy = false, -- or true, depending on whether you want it to load lazily
  --  init = function()
  --    vim.g.AirLatexUsername = 'cookies'
  --    vim.g.AirLatexCookie = os.getenv 'OVERLEAF_COOKIES'
  --  end,
}
